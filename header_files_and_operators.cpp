#include <iostream>
#include "something.h"
using namespace std;

int main(int argc, char const *argv[])
{
    cout << "Hello world" << endl; // endl means end line and jump to the new line . An alternative to \n .
}

// There are two types of header files :

// 1. System header files :  It comes with the compiler. EX. #include <iostream>
// 2. User defined header files : It is written by the programmer. EX. something.h which is in the same folder.

// Operators in C++ :

// following are the operators in C++ :
// 1. Arithematic operators : + , - , * , / , % , a++ , a-- , ++a , --a;

// 2. Assignment operators : = , used to assign value to variables.

// 3. Comparision operators : == , != , > , < , <= , >= . They return values in 0 and 1 like bool datatype . 0 for false and 1 for true .

// 4. Logical operators : && , || , ! (not logical operator (reverses the value for true to false and vise versa.) ).  They return values in 0 and 1 like bool datatype . 0 for false and 1 for true .